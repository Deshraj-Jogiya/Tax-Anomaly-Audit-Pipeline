name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  pipeline-smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Run the real pipeline end-to-end (Benford's Law + Isolation Forest + dashboard)
        run: |
          python etl/audit_etl.py
          python models/audit_detection.py
          python viz/dashboard_export.py
      - name: Run the same pipeline via Dagster asset orchestration
        run: |
          rm -f data/tax_compliance.db
          python -m dagster asset materialize -f dagster_pipeline.py --select raw_ledger_data,audited_ledger_data,compliance_dashboard
